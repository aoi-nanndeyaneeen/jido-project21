# 当日マニュアル (COMPETITION_DAY_MANUAL.md)

作成 2026-09-19。**通し練習をしないまま本番を迎える前提**で書いてある。

この紙だけで回せるようにしてある。背景や根拠は
[COMPETITION_RUNBOOK.md](COMPETITION_RUNBOOK.md) と
[COMPETITION_OPEN_ISSUES.md](COMPETITION_OPEN_ISSUES.md)、
コードの構造は [SYSTEM_FLOW.md](SYSTEM_FLOW.md)。

---

## 0. 役割 (3人)

| 役 | 持ち物 | 仕事 |
|---|---|---|
| **操縦者** | プロポ | 待機に入れる (スイッチ2つ) と bail-out。**それ以外は触らない** |
| **PC係** | ノートPC | ソフトの起動、**[G] で離陸**、画面の読み上げ。離陸後は **PC に触らない** |
| **コール係** | 目と声 | PC 画面の「次コール」を見て、審判へミッション名を叫ぶ |

コール係は PC 係が兼ねてもよいが、**声を出す人は必ず決めておく**。
ルール 6.4: 各ミッションは**開始直前のコールが無いと無効**。

---

## 1. 朝いちばん (30〜40分。会場に入る前でもよい)

### 1-1. ファームを2つ焼く ★今日変更があるので必須

```
cd flight_controller && pio run -e drone_s5_rp2040 -t upload
cd ../log_recorder   && pio run -e xiao_logger_esp32c3 -t upload
```

焼けたら機体のシリアル画面 (`pio device monitor`) で自己診断を見る。
**測距が `TSD20` と出て OK になっていること。** FAIL なら配線
(6ピンで 1番と5番は NC / I2C はストレート)。

### 1-2. 測距を定規で合わせる ★未実施の唯一の項目

TSD20 はベンチで 4.357m まで届くことを確認済みだが、**絶対値の校正だけ
まだやっていない**。機体を床に置き、シリアルの `鉛直h` と巻尺を比べる。

| 見る高さ | 合格 |
|---|---|
| 0.5 m | ±5cm |
| 1.0 m | ±5cm |
| 3.0〜3.6 m (机の上から天井向きでも可) | 値が出続ける・飛ばない |

ずれていたら `QuadConfig.h` の `RANGE_OFFSET_M` で平行移動させて焼き直す。
**3.6m が読めない場合は、飛行前に PC で [C] を押して上昇旋回を OFF にする。**

### 1-3. LED を見る ★機体審査の項目 (今日変えた)

ロガー基板のフルカラー LED が機体表示灯。

| 状態 | 色 |
|---|---|
| 電源ON / ディスアーム / 手動 (ANGLE) / POSHOLD | **赤 点灯** |
| GUIDED (自動飛行中) | **緑 2Hz 点滅** |

ルール 2.2.8 は「ハンズオフ中は青か緑で2Hz点滅、それ以外は赤点灯、
介入したら赤へ」。上の2状態しか無いのが正しい。
**黄色や青が出たら古いファームなので焼き直す。**

カメラ検知用の**白6Hz点滅は別物**で、併存してよい (2.2.8 の「他の発光機能」)。

---

## 2. 本番直前に決めるたった1つ: カメラを使うか

**準備は1分しかなく、超過は -50点/秒。** 基準5点のクリックまで終わる
見込みが立たないなら、迷わずカメラ無しで飛ばす。

| | カメラあり | カメラなし |
|---|---|---|
| 起動 | `cd position_estimator/src && python main.py` + ラズパイ `python camera_server.py` | `cd position_estimator/src && python fly_nocam.py` |
| 準備でやること | 2人で基準5点クリック | **何もしない (BLE が繋がるだけ)** |
| 移動 (進入・機動間) | カメラで位置を見て飛ぶ | **機首方向へ開ループ (時間で 6.3m)**、機動間はその場ホバー |
| 着陸 | その場 (静止をカメラでも確認) | その場 (静止は時間で判定) |
| 機動 (旋回・上昇旋回・8の字) | **機体単独。どちらでも全く同じ** | 同じ |

> **迷ったら `fly_nocam.py`。** 得点の大半 (旋回1800 + 上昇旋回3240 +
> 8の字5670) は機体単独の機動で、カメラは1点も稼がない。カメラが効くのは
> 「まっすぐ進入する」「機動の中心を戻す」だけ。

---

## 3. 準備時間 (1分)

1. 機体をバッテリー接続、**離着陸エリア②の滑走路に置く**。
   **機首はミッションエリアの方向へ**まっすぐ (これが進入方向になる)。
2. プロポON、**THR_CUT は入れたまま**。
3. PC で `python fly_nocam.py` (またはカメラ便の手順)。
4. 画面に出る**飛行プログラムの表**を一目見る。

```
  1 滑走路内離陸 (100点)         5s  20s
  2 ミッションエリアへ進入        11s  25s   … 位置が無ければ機首へ 6.3m 開ループ進入
  3 水平旋回 (1000点)           34s  45s   r=1.80m 2周 v=0.70
  4 定位置へ戻る                11s  20s
  5 上昇旋回 (1200点)           83s  95s   r=1.80m 5周 v=0.70
  6 定位置へ戻る                11s  20s
  7 8の字飛行 (1400点)          34s  45s   r=1.80m 2周 v=0.70
  8 滞空 (飛行継続ボーナス)        0s 240s   締切 210s まで居座る
  9 着陸 (その場) (300点)        11s  40s
```

**確認するのはここだけ:**
- `★はみ出す` が出ていない
- 上昇旋回を飛ばすなら 5 行目がある (OFF にしたければ **[C]**)
- 狭い場所で確かめたいだけなら **[S]** で 1/10 スケール

---

## 4. 飛行 (4分)

### 4-1. 手順 (★2026-09-19 に変更: スイッチでは離陸しない)

| # | 誰 | やること | そのとき何が起きるか |
|---|---|---|---|
| 1 | 操縦者 | **THR_CUT を解除** → アーム | PC が待機に入る (画面 `ARMING`) |
| 2 | 操縦者 | **スロットルを 15% 以上へ** | プロペラがアイドルで回る。まだ上がらない |
| 3 | 操縦者 | **SW_HOVER を up (GUIDED)** | 画面が `READY  ★[G]2回で離陸`。**機体はその場のまま。時計もまだ 0** |
| 4 | コール係 | **「離陸」とコール** | |
| 5 | PC係 | **[G] を 2 回** (3秒以内) | ★ここで競技時計が 0 から走る。離陸開始 |

3 で機体が動かないのが正常。**GUIDED に入っただけでは高度指令を出していない**
ので上がらない (以前はここで勝手に 0.5m まで上がっていた)。

離陸したら操縦者は着陸まで**何もしない**。プロポから手を離す
(ルール上、触ると終了)。PC係も [G] の後は触らない。

> **[G] を押しても上がらないとき**は、スロットルが 15% 未満。
> 機体のシリアルに `離陸指示は保留: スロットルが ○%` と出る。
> 操縦者がスロットルを上げれば、押し直さなくてもそのまま離陸する。

### 4-2. コール係のタイムライン

PC 画面の右に **`次コール「○○」`** が常に出ている。それを読み上げるだけ。
目安の時刻 (カメラ無しの場合。カメラありはこれより数秒早い):

| T+ | 出来事 | コール |
|---|---|---|
| 0s | 離陸 | **「離陸」** ([G] を押す直前に。待たせても時計は走らない) |
| 〜12s | ミッションエリアへ進入 | — |
| 12s | 水平旋回 開始 | **「水平旋回」** (T+10s あたりで) |
| 44s | 旋回終了、位置戻し | — |
| 64s | 上昇旋回 開始 | **「上昇旋回」** (T+60s あたりで) |
| 145s | 上昇旋回 終了 | — |
| 165s | 8の字 開始 | **「8の字飛行」** (T+160s あたりで) |
| 197s | 機動終了、**滞空** | — |
| 210s | 着陸開始 | **「着陸」** |
| 〜217s | 静止5秒を確認して完了 | — |

**滞空はサボりではない。** 飛行継続ボーナスは 10点/秒 (5ミッション時) で
倍率の対象外なので、**早く降りた秒数がそのまま失点**になる。
規定の締切は 235s、こちらは 217s に降り切る設計で 18秒の余裕がある。

### 4-3. 中断のしかた (上から順に軽い)

| やること | 何が起きる |
|---|---|
| PC の **[X]** | その場から自動着陸 (プログラムを打ち切る) |
| **SW_HOVER を cen** | 即 POSHOLD。PC は指令を止めて手を引く |
| **SW_HOVER を down** | 即 ANGLE (完全手動)。**これが bail-out** |
| **THR_CUT** | モーター停止。最終手段 |

**明確な危険があるのに手動へ切り替えないと即失格** (ルール)。
迷ったら SW_HOVER を down。

---

## 5. 症状別の対処

| 症状 | 原因 | その場でやること |
|---|---|---|
| アームしても PC が `ARMING` にならない | BLE が繋がっていない | PC を再起動。`ble_monitor.py` や `main.py` が裏で動いていたら落とす (同時接続できない) |
| `ARMING` のまま `READY` にならない | 機体が GUIDED に入れない | SW_HOVER が up か / スロットル15%以上か / フローと測距が生きているか。機体のシリアルに理由が出る |
| `READY` で [G] を押したのに上がらない | スロットルが 15% 未満 | 操縦者がスロットルを上げる。**押し直さなくてよい** (機体が待っている) |
| `READY` のまま勝手に `ARMING` へ戻る | 機体が GUIDED を抜けた (スティックに触った / フローか測距が落ちた) | SW_HOVER を cen → up で入れ直す |
| 離陸直後に降りてしまう | 位置ロスト判定 (カメラ便のみ) | `fly_nocam.py` に切り替える。こちらはこの判定を見ない |
| 高度がどんどん上がる / 天井に張り付く | 測距が目標高度を読めていない | **[C] で上昇旋回 OFF**。SW_HOVER down で回収 |
| 円が歪む・途中で止まる | 測距の飛び (コーン・ハードル) | 止められない。次のトライで機動の中心をずらす |
| 8の字の2周目 (逆回り) が回らない | **未検証。完走記録が無い** | 放置してよい。制限時間45sで打ち切って次へ進む。倍率は1段ずれるだけ |
| 段階が進まない | どこかで粘っている | 触らない。段階ごとの制限時間で必ず次へ行く |
| 機体が流れていく | フローのドリフト | SW_HOVER down で bail-out |

---

## 6. トライ間 (1分) にやれること

書き込みも設定変更も許されている。**キー操作だけで済むものを優先**:

| 変えたいこと | やり方 |
|---|---|
| 上昇旋回をやめる / 戻す | PC で **[C]** |
| 全体を 1/10 に縮める (場所が狭い) | PC で **[S]** |
| 進入距離を変える (カメラ無し) | `config.py` の `COMP_ENTRY_DR_S` (秒)。距離 = 0.7 × 秒 |
| 旋回半径・速度 | `config.py` の `COMP_TURN_RADIUS_M` / `COMP_MANEUVER_SPEED` |
| 着陸を離陸地点へ戻す (400点狙い) | `config.py` の `COMP_RETURN_HOME = True` |
| 各段階の制限時間 | `config.py` の `COMP_BUDGET_S` |

**config.py を触ったら必ず**
`cd position_estimator && python -m unittest tests.test_mission_sim`
(20件。半径・締切・順序・フェンス・カメラ無しの経路を機械的に検算する)。

---

## 7. 2026-09-19 に入れた変更

| # | 変更 | なぜ |
|---|---|---|
| 1 | **帰投をやめ、その場で着陸** (`COMP_RETURN_HOME=False`) | 帰投は一番ドリフトが溜まった状態での長距離移動で、失敗すると着陸 1822点を巻き添えにする。素点の差は 400-300 だけ |
| 2 | **滞空の段階を追加** | 飛行継続ボーナス 10点/秒。機動が早く終わったぶんを使い切る。上昇旋回を OFF にしても時間を捨てない |
| 3 | **締切を 210s / 218s に** | 帰投が無くなったぶん遅らせた。接地 212s + 静止5s = 217s (規定 235s) |
| 4 | **カメラ無しでも進入する** (`COMP_ENTRY_DR_S`) | ルール 6.7 の離陸は「ミッションエリアに進入すること」まで含む。その場ホバーでは離陸 120点 と後続の倍率が全部ずれる |
| 5 | **カメラ無しの便で「位置ロスト→着陸」を切った** | 位置が最初から無いので、離陸直後に必ず着陸していた (`fly_nocam.py` が成立しないバグ) |
| 6 | **機体フェンスを ±10m に** | 旧値 2.7/4.2m は練習場のもの。8の字が ±3.6m を使うので余裕 0.6m しか無く、**機動の途中で押し返されて円が崩れる**。本番のミッションエリアは約22m四方 |
| 7 | **LED をルール 2.2.8 に合わせた** | ハンズオフ=緑2Hz点滅 / それ以外=赤点灯。従来は 黄・青・緑4Hz で不適合。機体審査の項目 |
| 8 | **次にコールするミッションを画面に出す** | ルール 6.4。コール漏れ = ミッション無効 |
| 9 | **GUIDED に入れても離陸しない。離陸は PC の [G]** | スイッチ一発で 4 分の自動飛行が始まるのは、コール (ルール 6.4) のタイミングも握れず危ない。旧実装は地上で測距が無効なため `ALT_TARGET_M`=0.5m が入り、**PC の意図と関係なく上がっていた**。待機中はリンク断の自動着陸も走らない。LED も待機中は赤 (ハンズオフではないため) |

---

## 8. 早見表

```
半径 1.8m   速度 0.7m/s   1周 16.2s
巡航高度 0.5m   上昇旋回の到達高度 3.6m   フェンス天井 4.0m
着陸開始 210s   強制着陸 218s   規定の締切 235s
想定: 接地 212s / 静止完了 217s

得点 (1トライ目)
  離陸 120 / 水平旋回 1800 / 上昇旋回 3240 / 8の字 5670 / 着陸 1822
  + 飛行継続 約2100  =  約14700点

キー   G=離陸(2回押し)  S=1/10スケール  C=上昇旋回ON/OFF  X=中断着陸  Q=終了
プロポ THR_CUT解除 → スロットル15% → SW_HOVER up (ここまでは上がらない)
離陸   コール「離陸」→ PC係が [G][G]
bail-out  SW_HOVER down
```

### 未検証のまま飛ばすもの (承知のうえで)

1. **8の字の逆回り** — 完走記録が無い。ここだけは当日が初回
2. **3.6m での高度保持** — TSD20 は届くがホバー実績なし。怪しければ [C]
3. **地上局ミッション経路そのもの** — 復活させてから一度も飛んでいない
4. **半径1.8m / 速度0.7m/s** — 実機は 1.0m / 0.8m/s までしか飛んでいない

1〜3 はどれも**落としても倍率チェーンは切れず、後続が1段ずれるだけ**。
着陸だけは守ること。着陸を落とすと倍率ごと 1822点 が消える。
