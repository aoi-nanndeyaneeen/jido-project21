# 本番 検討事項 (ルールブック突き合わせ)

作成 2026-09-18。出典は **全日本学生室内飛行ロボットコンテスト 完全自動部門
ルールブック 2026/5/18 Version 1.1**。

手順そのものは [COMPETITION_RUNBOOK.md](COMPETITION_RUNBOOK.md)、コードの構造は
[SYSTEM_FLOW.md](SYSTEM_FLOW.md)。**この文書は「ルールと実装が食い違っている所」と
「まだ決めていない所」だけを集めたもの**で、決着したものは RUNBOOK 側へ移すこと。

---

## 0. 決まっていること

| 項目 | 決定 | 根拠 |
|---|---|---|
| 地上局からの自動指令 | **使う** (2026-09-18 確認済み) | 6.3 の「送信機を使わず機体へ指示」条項は適用されないと確認 |
| 自動着陸滑走路 (800点) | **狙わない** | 離陸は離着陸エリア②、自動着陸用滑走路は反対側の離着陸エリア①。約30m の横断が要る |
| 着陸の狙い | **飛行競技エリア内 300点** | ミッションエリアでその場着陸。倍率込みで 1822点 |
| 飛行時間 | **使い切る** | 飛行継続ボーナスが 10点/秒 (5ミッション時)。早く降りるほど損 |

---

## 1. ルールの要点 (実装に効くものだけ)

### 1-1. フィールド (3.1 / 3.3、図3.2)

```
全体 42m x 26m   上下にマージナルエリア 2m ずつ
  離着陸エリア① (左・幅10m)   : 操縦者が入る。自動着陸用滑走路がある
  ミッションエリア (中央)      : おおよそ 22m x 22m。マーカーコーンが散布される
  離着陸エリア② (右)          : ここの滑走路から離陸する
  物資投下エリア (中央)        : 外周をミニハードルで囲まれている
設備: 滑走路 5m x 90cm / 高さ1cm、ポール 高さ3m を2本、
      マーカーコーン φ20cm 高さ6cm、ミニハードル 高さ15cm
```

**`utils/config.py` の `FIELD_PROFILE = "large"` (6m x 9m) は実寸ではない。**
機動の footprint は 3m x 6m なので、22m 四方に対してフェンスの余裕は大きい。

### 1-2. 得点 (6.5 / 6.6)

- **連続ボーナス**: そのトライで n 個目の成功ミッションに `1.5^(n-1)` を掛ける
- **初回ボーナス**: 各ミッションの**初成功のみ** 1.2 倍 (2回目以降は無し)
- **飛行継続ボーナス**: `飛行時間[s] x 2 x そのトライの成功ミッション数`。
  倍率の対象外。飛行時間はトライ開始から**最初の接地**まで

| 順 | ミッション | 素点 | 連続 | 初回 | 得点 |
|---|---|---|---|---|---|
| 1 | 滑走路内離陸 | 100 | 1.0 | 1.2 | 120 |
| 2 | 水平旋回 連続2回 | 1000 | 1.5 | 1.2 | 1800 |
| 3 | 上昇旋回 | 1200 | 2.25 | 1.2 | 3240 |
| 4 | 8の字飛行 | 1400 | 3.375 | 1.2 | 5670 |
| 5 | 着陸 (エリア内) | 300 | 5.0625 | 1.2 | 1822 |
| | 飛行継続 (230s x 2 x 5) | | | | 2300 |
| | **1トライ計** | | | | **14952** |

**トライは3回**あり、3トライの合計点で順位が決まる。2・3トライ目は初回ボーナスが無いので
約 12800。3本揃えて **約4万点**。つまり**再現性が最大の価値**で、1回きりの
最適化より「同じものが3回動く」ことの方が効く。トライ間は1分あり、書き込み可。

### 1-2b. ミッションを1つ落としたときの被害額

倍率が後続にずれるので額面より高くつく。上昇旋回を落とした場合:

```
成功時: 120 + 1800 + 3240 + 5670 + 1822 = 12652
失敗時: 120 + 1800 +  --  + 3780 + 1215 =  6915   (差 5737点)
```

**順序は現行のままが最適。** 失敗時の合計は順序によらず同じで、成功時は
「素点の小さい順」(1000 → 1200 → 1400) が最大になる。並べ替える理由はない。

### 1-3. 各ミッションの成立条件

- **滑走路内離陸 (6.7)**: 滑走路以外に接地せずに、**離着陸エリア②からミッション
  エリアに進入すること**。→ ミッションエリア進入は必須。省略できない
- **水平旋回 (6.8)**: 旋回半径 概ね1.5m 以上。高度変化が十分小さいこと。
  超信地旋回は不可。1回400 / 2回800 / **連続2回1000**。1チャレンジ最大2回
- **8の字飛行 (6.9)**: 半径1.5m以上で1周 → 逆向きに1周。
  **2つの旋回半径が著しく異なると認めない**
- **上昇旋回 (6.10)**: ①**3m以下**を保って半径1.5m以上で**2周** →
  ②**ポールの高さ (3m) 以上**へ上昇 → ③**3m以上を保って2周**。
  付記: **機体の一部でも指定高度領域を違反したら認めない**
- **着陸 (6.13)**: 「着陸」コールの後に着陸静止。コール前の接地は失敗。
  飛行競技エリア内 300 / 離着陸エリア内 400 / 自動着陸滑走路内 800。
  **静止は5秒以上**。したがって **3分55秒 (235s) 以降の着陸は失敗**

### 1-4. 進行 (第5章)

- **競技準備 1分**。超過は **-50点 x 延長秒数** (最大60秒まで)
- 機体を離着陸エリア②の滑走路に置く。送信機は操縦者の前の台に**静置**
- **ミッション申告制 (6.4)**: 各ミッションの開始直前に操縦者がミッション名をコール。
  コール後、そのミッションの成否が決まるまで次へ進めない。
  審判が聞き取れないと無効または時間計測対象外
- **トライ終了条件 (6.3)**: 接地かつ静止 / **操縦者が送信機に手を触れた** /
  マージナルエリアを飛んだ / 飛行競技エリア外を飛んだ / 4分経過
- **即失格**: 明確な危険があるのに手動へ切り替えなかった場合

### 1-5. 発光 (2.2.8)

- ハンズオフ飛行中: **青または緑で 2Hz 程度の点滅**
- それ以外: **赤点灯**。審判から視認できない場合は赤とみなされる
- 操縦者の介入があったときは赤点灯へ遷移
- 競技に支障のない範囲で他の発光機能を持ってよい (= カメラ検知用LEDは別に持てる)

---

## 2. 実装とのギャップ (要修正)

| # | 項目 | ルール | 現状 | 影響 | 対応 |
|---|---|---|---|---|---|
| G1 | 上昇旋回の高度 | ポール高さ **3m 以上**で2周、低い方は3m以下で2周 | `COMP_CLIMB_ALT_M = 2.20`。`RANGE_MAX_M = 3.5` で clamp、VL53L1X Medium の実用は ~2.9m | **上昇旋回 3240点が丸ごと 0** | 測距をソナー化 (§3 A1)。低2.0m / 高3.6m を目標に |
| G2 | 発光の色 | 赤点灯 / 青・緑2Hz点滅の区別 | **配線は足りている**。`STATUS_LED_VIA_LINK=true` でロガー側のフルカラー LED を `T_LED`/`LedFrame` (R/G/B ビット) で光らせている。`BOARD_BLINK_R/G/B=D8` は別物 (カメラ検知用の白6Hz) | 色のポリシー未定のみ | どの状態で何色かを決めて実装する。2Hz 点滅の生成も含む |
| G3 | **段差ゲート ★実害あり** | ミニハードル 15cm・コーン 6cm を跨ぐ | `RANGE_STEP_M = 0.15f` はハードルと同値で素通りする | **既に機動を3回潰している最大の中断要因**: LOG0042 (0.48→0.14m) / LOG0045 (0.23→0.56m、**8の字が死んだ直接原因**) / LOG0060 (0.47→0.23m)。本番のミッションエリアにはコーンが散布されるので確実に再発する | しきい値を 0.10 程度へ。既存ログで誤検出率を確認 |
| G4 | フィールド寸法 | 42x26m、ミッションエリア 約22m四方 | `FIELD_PROFILE = "large"` は 6x9m | カメラのキャリブ・フェンス・幾何チェックが全部ずれる | 実寸のプロファイルを新設。フェンスは余裕が増える方向 |
| G5 | 着陸点 | エリア内 300点で狙う | `program.py` は離陸地点へ帰投して 800点狙い | 帰投は不要。むしろドリフトの主因 | 帰投ステップを削除。その場着陸へ |
| G6 | 飛行時間 | 継続ボーナス 10点/秒。235s まで飛べる | `COMP_TARGET_S = 180` で早期完了を志向 | 約 1000点 の取りこぼし | 機動後に滞空ステップを追加。締切は 235s 基準に引き直す |
| G7 | ミッション申告 | 開始直前に人がコール。コール後は成否まで次へ進めない | 機体/PCが勝手に次段階へ進む | コール漏れ = ミッション無効 | 機体の段階を人に見せる表示 (PC/LED/ブザー)。機体側は「コール待ち」で止まれること |
| G8 | 水平旋回の回数 | 連続2回で1000点。最大2回 | `COMP_CIRCLE_LAPS = 2` | 問題なし | 確認のみ |
| G9 | **旋回半径** | 水平旋回・8の字・上昇旋回とも**概ね1.5m以上** | 機体単独の値が `CIRCLE_RADIUS_M=1.0` / `FIG8_RADIUS_M=0.50` / `CLIMB_RADIUS_M=0.75` (いずれも屋内調整用) | **3ミッションとも不成立 = 10710点** | §3 A0。R=1.8 / V=0.7 を提案 |
| G10 | 軌道追従の速度余地 | — | 軌道追従の目標速度は `FF + 位置補正(±0.30)` を `FLOW_STICK_VEL=1.0` でクランプ (PosHold.h:235) | V=0.8 だと補正余地が +0.2 しか無く、追従が飽和して**実半径が縮む** | V ≤ 0.7 にする |

---

## 3. 未決事項 (要判断)

### A0. 旋回半径 ★最優先 (机上で確定できる)

**現在値は3つとも規定未満。** `CIRCLE_RADIUS_M=1.0` / `FIG8_RADIUS_M=0.50` /
`CLIMB_RADIUS_M=0.75`。いずれも屋内調整用の値で、このまま飛ばすと
水平旋回・8の字・上昇旋回が全部不成立になる。

**半径がどう決まるか**: `CircleTrack` は中心を固定して円周上の目標点を作り、
`PosHold` が位置 P で追う。半径は幾何的に指令値そのもの (開ループの `Maneuver.h`
のように r=v/ω の誤差が直接半径にならない)。実半径を動かすのは 2 つだけ:

- **(a) 速度の飽和**: 軌道追従の目標速度 = `FF(V) + 位置補正(±CIRCLE_POS_VEL_LIM=0.30)`
  を `FLOW_STICK_VEL=1.0` でクランプ (PosHold.h:235)。V=0.8 では補正余地が +0.2
  しか無く、飽和すると円の内側を通る = **実半径が縮む**。→ **V ≤ 0.7**
- **(b) 高度スケール誤差**: フロー速度 = 角流量 × 高度 なので、位置推定が高度比で
  まるごとスケールする。**実半径 = 指令半径 × (真の高度 / 推定高度)**。
  推定高度が低め (ToF クランプ、コーン/ハードルの上) なら実半径は大きくなり安全側。
  **推定高度が高めに出ると実半径が縮んで違反側**。上昇旋回の高高度側で最も効く

**実測 (2026-09-18 解析済み。`analyze_circle.py`、完走した周のみ)**

| ログ | 指令R | 実測R | 差 | 半径リップルRMS | 起点へのずれ |
|---|---|---|---|---|---|
| LOG0042 | 1.00 | 0.96 | -4% | 0.085 | (未完了) |
| LOG0044 | 1.00 | 0.97 | -3% | 0.096 | 0.03 m |
| LOG0057 ①低 | 0.75 | 0.69 | **-8%** | 0.136 | 0.02 m |
| LOG0057 ②低 | 0.75 | 0.76 | +1% | 0.140 | 0.26 m |
| LOG0057 ③上昇 | 0.75 | 0.81 | **+8%** | 0.230 | 0.40 m |
| LOG0057 ④高 | 0.75 | 0.76 | +1% | 0.173 | 0.33 m |
| LOG0057 ⑤高 | 0.76 | 0.79 | +4% | 0.108 | 0.33 m |

**`CircleTrack` のクローズドループは効いている (-8%〜+8%)。**
古いメモの「実測は指令より2〜3割大きい」は開ループ (`Maneuver.h`) 時代の話で、
現行パスには当てはまらない。

```
R = 1.5 指令 → 最悪 -8% → 1.38 m   ← 規定を割る
R = 1.8 指令 → 最悪 -8% → 1.66 m   ← 安全
R = 2.0 指令 → 最悪 -8% → 1.84 m   ← より安全
```

**推奨: R = 2.0 m / V = 0.7 m/s**

半径を大きくしても損はしない。**継続ボーナスは飛行時間そのものに付くので、
大きい円で時間を使っても、小さい円で回って余った時間を滞空に使ってもボーナスは同じ。**
したがって時間が許す限り大きく取るのが合理的。

```
R=2.0 / V=0.7 : 1周 18.0s x 9周 = 161.6s + 離陸・進入 35s = 約197s (235sまで38s余裕)
R=1.8 / V=0.7 : 1周 16.2s x 9周 = 145.4s + 35s          = 約180s (55s余裕)
R=2.2 / V=0.7 : 1周 19.8s x 9周 = 177.7s + 35s          = 約213s (22s。詰めすぎ)
```

必要リーン角は atan(V²/(R·g)) = 1.4° (R=2.0)。`FLOW_MAX_LEAN=8°` に対し十分。
8の字の所要長 4R = 8.0 m (ミッションエリア22m四方なので問題なし)。

**★ 8の字は一度も安定して回れていない (LOG0045〜0065 全確認済み、2026-09-18)**

完走しているのは **右旋回1周だけ**。左旋回はどれも 29〜33° で中断しており、
1周回れた記録が無い。

| ログ | 中身 |
|---|---|
| LOG0059/0062/0063/0065 | GUIDED の周回区間なし (0063・0065 は 32B の空ファイル) |
| LOG0060 | 左旋回 33°(中断) → 右旋回 -1°(**測距の飛び**) → 右旋回 **1周完走** R=0.72 (指令0.75) |
| LOG0061 | 左旋回 29°(中断) → 右旋回 88°(中断) → 右旋回 94°(中断) → 右旋回 **1周完走** R=0.71 |
| LOG0064 | 右旋回4本。3本目から高度推定が壊れ半径誤差 RMS 0.734 / 最大 1.52m まで発散 |

唯一の8の字 (LOG0045) の中身:

```
実測進行 185 deg (目標 215) で中断   ← 測距の飛び 0.23 → 0.56 m
フィット半径 0.64 m (指令 0.69)
半径誤差   平均 +0.298  RMS 0.567  最大 1.100 m
半径の揺れ 振幅 RMS 0.483 m  ピーク 0.07 Hz
ヨー誤差   平均 -263.8  RMS 277.2 deg      ← 機首が接線に全く乗っていない
```

半径 0.64m に対して半径誤差 ±0.5m = **円の形になっていない**。8の字は途中で旋回方向を
反転させる (機首の目標が不連続に振れる) ので、**反転の処理が効いていない疑いが濃い**。

**ただし「8の字だから」ではなく「半径が小さすぎるから」の可能性が高い**:

| 指令半径 | 半径誤差 平均 | リップル RMS |
|---|---|---|
| **0.50 m** (LOG0045・8の字) | **+0.298 (+60%)** | **0.483** |
| 0.75 m (LOG0057/0060/0061/0064) | -8% 〜 +8% | 0.11 〜 0.26 |
| 1.00 m (LOG0042/0044) | -3% 〜 -4% | 0.085 〜 0.096 |

小さい円ほど求心加速度もヨーレートも大きく、フローの誤差が効く。**R=2.0 に上げれば
大幅に改善する見込みはある**が、**反転の処理が効くかは半径では解決しない**。
ここは飛ばさないと分からない。ルール 6.9 の「2つの旋回半径が著しく異なる場合は
認めない」に直接かかる。**1400点+倍率のリスク。**

**★ 高度誤差 → 半径誤差 の実証 (LOG0064)**: 高度推定が壊れた周では

```
1周目: 実測R 0.74 (指令0.75)  半径誤差RMS 0.192  起点ずれ 0.25m
2周目: 実測R 0.89 (指令0.86)  半径誤差RMS 0.259  起点ずれ 1.60m
3周目: 実測R 1.00 (指令0.98)  半径誤差RMS 0.734  最大 1.52m
```

フロー速度 = 角流量 x 高度 なので **高度が壊れると円が壊れる**。
3m超の高度問題 (A1) は「上昇旋回が成立するか」だけでなく、
**高高度2周の半径が規定を満たすかの問題でもある**。

**多周回のドリフト**: LOG0057 の起点へのずれは 0.02 → 0.26 → 0.40 → 0.33 → 0.33 m と
3周目あたりで飽和する。5周の上昇旋回でも円の中心は 0.4m 程度しか動かない。
22m四方なので問題にならない。

### A1. 3m超の高度をどう測るか ★最優先

**試験がほとんどできない前提での評価 (2026-09-18)。判断基準は「飛ばさずにどこまで
確認できるか」。**

| 案 | 新規要素 | ベンチで確認できるか | 評価 |
|---|---|---|---|
| ToF を Long + `RANGE_MAX_M` 引き上げ | 設定値のみ | **できる** (3.6m先の床/天井を読むだけ) | **まずこれ**。ピン0本、戻すのも容易 |
| ソナー (`RANGE_BACKEND=Sonar_EZ`) | 配線のみ。**ドライバは実装済み** | できる (同上) | 次点。「新しいセンサ」ではない |
| 気圧計・別のLiDAR | ドライバ + 融合 + 調整 | **できない** | **入れない** |

**測距が「届くか」はベンチで確認できる。飛ばないと分からないのは「3.6mで保持できるか」
だけ。** ここは切り分けられる。


- VL53L1X: **室内なので日光が無く ToF に有利**。`Long` モードなら 3.5〜4m 圏。
  精度とレートは落ちる。`RANGE_MAX_M` の引き上げも要る
- ソナー LV-MaxSonar-EZ: 素の仕様は 254インチ = **6.45m**。`RANGE_SONAR_MAX_M = 4.00`
  は保守値。体育館の硬い床は超音波に理想的。**本命**
- **VL53L1X は I2C (Wire) なので追加ピンが要らない。** Long 化 + `RANGE_MAX_M` の
  引き上げは**ピン0本**で試せる。→ **まずこれ**
- ソナーに行く場合のみ PW 用のピンが要る。XIAO RP2040 の空きは D9/GP4 の1本
  (BoardPins.h)。ただし **`FLOW_VIA_LINK` (T_FLOW/FlowFrame) と
  `STATUS_LED_VIA_LINK` (T_LED/LedFrame) の前例があるので、`RANGE_VIA_LINK` +
  `T_RANGE` でロガー (ESP32C3) に載せられる**。LED は既にロガー側なので競合しない
- ただし測距は AltHold に入る制御信号。`AltEstimator.h` 冒頭のとおり高度ループは
  遅れに弱い (420ms で「遅くて振れる」)。**リンク経由にするなら
  `ALT_USE_ACC_FUSION` の有効化とセットで検討する**
- 3.6m では PMW3901 の速度スケールが 1m の 3.6倍ばらつく。
  **半径1.5mの円が高高度で崩れないかは実測必須**
- 測距が飛ぶと `RangeLost` で基準スロットル保持 → 高度が流れる。
  **`ALT_USE_ACC_FUSION` (今 false) をブリッジとして有効化する価値がここで出る**

### A2. マーカーコーンとミニハードルへの対処

- **着陸地点**: コーンの上に降りると転倒し、静止5秒が成立しない (1822点が飛ぶ)。
  コーンは大会設置物なので動かせない。→ **準備時間中に目視でコーンの無い場所を
  選び、そこを着陸目標にする運用**。カメラを使うならここが一番価値が高い
- **機動の位置取り**: 物資投下エリアはハードルで囲まれている。8の字は 3m x 6m
  なので、**投下エリアとハードル環を避けた中心**を選ぶ。22m四方なので場所はある
- **測距**: ハードル15cm は G3。コーン6cm はゲートを通過して高度に入るが、
  0.8m/s なら通過 0.25秒なので実害は小さいはず (低高度ほど効く)
- **フローには有利**: コーンは模様として読めるので、のっぺりした床より良い

### A3. 発光要件の実装先 (配線は解決済み)

- **既にロガー側のフルカラー LED を `T_LED`/`LedFrame` で光らせている**
  (`STATUS_LED_VIA_LINK = FLOW_VIA_LINK = true`)。機体側のピンは消費しない
- 残っているのは**色のポリシー**だけ: ハンズオフ中は青か緑で 2Hz 点滅、
  それ以外は赤点灯、介入時は赤へ遷移。2Hz の生成をどちら側でやるかも決める
- カメラ検知用の 6Hz 白 LED (`BOARD_BLINK_*` = D8) は別物で、
  「他の発光機能」として併存可

### A4. 離陸位置と機動中心の設計

- 離陸は離着陸エリア②の滑走路 (5m x 90cm)。そこからミッションエリアへ進入
- 進入後、機動の中心まで何m移動するかが未定。**投下エリアとコーンの実配置を見て
  当日決める**部分が残る。移動距離が短いほどフロー積分の誤差が小さい

### A5. カメラをどこに使うか

- リンクは使えるので技術的な制約は無い。ただし**ミッションエリアが約22m四方**で、
  6x9m 前提のキャリブ・レンズでは追えない可能性が高い
- 準備超過は **-50点/秒**。キャリブが押すと直接失点する
- **飛行そのものは機体単独で完結させ、カメラは「着陸地点の選定」と「記録」に
  絞る**のが費用対効果が高い、というのが現時点の見立て

---

## 4. 当日の運用で決めること

- 機動の中心をどこに置くか (投下エリア・コーンの実配置を見て)
- 着陸地点 (コーンが無く、ミッションエリア内であること)
- 機首の向き (8の字の 4R = 6.0m をどちらに並べるか。22m四方なので制約は緩い)
- ミッションコールのタイミングと担当 (機体の段階表示を誰が見るか)
- トライ間1分で何を直せるようにしておくか (書き込み可)

---

## 5. 次にやること (順序)

飛行試験ができない間は 1〜4 (すべて机上) を先に進める。

1. ~~**A0-1**: 実測半径を出す~~ **完了 (2026-09-18)**。LOG0045 は失敗ログだったが
   LOG0057 に **5周の上昇旋回の完走**があり、そこから -8%〜+8% と判明。
   **左旋回の完走データはゼロのまま (8の字2周目が未検証)**
2. **A0-2**: 半径・速度を **R=2.0 / V=0.7** に引き直す
   (`CIRCLE_RADIUS_M` / `FIG8_RADIUS_M` / `CLIMB_RADIUS_M` / 各 `*_SPEED_MPS`)
3. **G1/G5/G6**: 高度 (低2.0m / 高3.6m)・締切 (235s 基準)・プログラム構成を引き直す
4. **G3 ★前倒し**: 段差ゲートのしきい値を 0.15 → 0.10 前後へ。既存ログで誤検出率を確認。
   測距の飛びは既に機動を3回中断させており、8の字が死んだ直接原因でもある
5. **A1**: 測距を ToF Long + `RANGE_MAX_M` 引き上げで試す (ピン0本)。
   足りなければソナーを `RANGE_VIA_LINK` で ESP32C3 へ ← **飛行試験が要る最大の未知数**
6. **G2/A3**: 発光の色ポリシーを決めて実装する (機体審査に必要)
7. **G4**: 実寸のフィールドプロファイルを作る
8. **G7**: 段階表示とコール運用を決める
