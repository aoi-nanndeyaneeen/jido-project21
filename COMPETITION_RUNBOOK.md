# 本番手順書 (COMPETITION_RUNBOOK.md)

作成 2026-09-17。**準備 1 分 / 飛行 4 分** の本番で、離陸から着陸までを全自動で
通すための手順と、そのために何がどう設定されているか。

コードの構造と周期は [SYSTEM_FLOW.md](SYSTEM_FLOW.md)、段階的な立ち上げは
[WAYPOINT_BRINGUP.md](WAYPOINT_BRINGUP.md)、手動コンソールは
[GROUND_CONSOLE_GUIDE.md](GROUND_CONSOLE_GUIDE.md)。

---

## 0. 当日の持ち物チェック (競技前)

| 項目 | 確認 |
|---|---|
| ファーム | 機体 `pio run -e drone_s5 -t upload` / 地上局 `pio run -e xiao_s5_log -t upload` を**両方**焼いてある |
| プロトコル | 焼いた 2 つが同じ `protocol/` から作られている (違うと `# !! パケットバージョン不一致`) |
| フィールド | `utils/config.py` の `FIELD_PROFILE = "large"` (6m x 9m) が実寸と合っている |
| 基準点 | `FIELD_POINT_COORDS["large"]` の 4 隅と 5 点目の高さが実寸と合っている |
| 機体側フェンス | `QuadConfig.h` の `FENCE_E_LIM/N_LIM` と `config.py` の `COMP_FENCE_X/Y` が同じ |
| 電池 | 3 分飛べるものを満充電で |

---

## 1. 準備前 (待機状態)

1. 機体にバッテリーを接続。プロポの電源を入れる。**THR_CUT は入れたまま**。
2. 機体を**離着陸エリアに、機首をフィールド右 (+x) へ向けて**置く。

> ### ★ 機首の向きが重要な理由
>
> 定型機動は機体座標で「前進 + ヨーレート」を出すだけなので、地面に描かれる円の
> 位置は機首の向きで決まる。とくに **8の字は機首の左右に円を 2 つ並べる**ので、
> 機首に直交する向きに **4R = 6.0m** 必要になる。
>
> * 機首が +y (フィールド奥) … 4R は x 方向 = 幅 6m ちょうど → **確実にはみ出す**
> * 機首が +x (フィールド右) … 4R は y 方向 = 9m → 収まる (実際は ±3.0m)
>
> `config.py` の `YAW_INITIAL_ALIGN_DEG = 90.0` がこの前提。起動時に
> `main.py` が実寸で検算して一覧を出すので、`★はみ出す` が出ていないか必ず見ること。

---

## 2. 準備時間 (1 分)

2 人で並行して進める。**どちらが先でもよい** (PC 側が最大 60 秒ラズパイを待つ)。

| 担当 | やること |
|---|---|
| ラズパイ係 | `python camera_server.py` |
| PC 係 | `cd position_estimator/src` → `python main.py` |

1. PC に `[WAIT] Camera2 (RPi ...) を待っています` が出たら、ラズパイの起動待ち。
   繋がると `[OK] Camera2 (RPi) 到達確認` に変わる。
2. **2 人で同時に基準 5 点をクリック**する。PC は Camera1 の画面、ラズパイは
   ラズパイの画面。順序はどちらも画面の指示どおり
   (手前左 → 手前右 → 奥右 → 奥左 → 奥左の真上)。
   * ラズパイ側は接続前に押した点も溜めているので、PC が Camera1 を
     クリックしている間に押しておいてよい。
3. 両方そろうと再投影誤差と 2 カメラ整合が出て、追跡が始まる。
4. 続けて**飛行プログラムの一覧とフェンスの検算**が出る。ここを一目見る。

```
  ---- 飛行プログラム (本番) ----
  # 段階                    想定    制限   累計(制限)  備考
  1 滑走路内離陸 (100点)              5s    20s      20s
  2 ミッションエリアへ進入               11s    25s      45s   機動の開始地点 (機首から計算)
  3 水平旋回 (1000点)              26s    40s      85s   r=1.50m 2周 v=0.80 w=+30.6deg/s
  4 定位置へ戻る                    11s    20s     105s   機動の開始地点 (機首から計算)
  5 上昇旋回 (1200点)              61s    80s     185s   r=1.50m 5周 v=0.80 w=+30.6deg/s
  6 定位置へ戻る                    11s    20s     205s   機動の開始地点 (機首から計算)
  7 8の字飛行 (1400点)             26s    40s     245s   r=1.50m 2周 v=0.80 w=+30.6deg/s
  8 離着陸エリアへ帰投                 10s    30s     275s   離陸地点 (地上で見えていた位置)
  9 着陸 (800点)                 11s    40s     315s
  想定合計 172s  (目標 180s / 着陸開始の締切 185s / その場着陸 205s)

  機首 +90deg のとき、各機動が使う範囲 (フェンス x±2.7 y±4.2 m):
    水平旋回         x[-1.50,+1.50] y[-1.50,+1.50]  OK
    上昇旋回         x[-1.50,+1.50] y[-1.50,+1.50]  OK
    8の字飛行        x[-1.50,+1.50] y[-3.00,+3.00]  OK
```

**ここで `★はみ出す` が出ていたら飛ばさない。** 円が境界で押し返されて崩れ、
ミッションが判定されない。機首の向きか `COMP_TURN_RADIUS_M` を直す。

> 準備が 1 分より早く終わったら、そのまま次へ進んでよい。

---

## 3. 飛行 (4 分。目標 3 分で完了)

**PC にはもう触らない。** 操作はプロポだけ。

1. **THR_CUT を解除**してアーム
   → PC が自動でミッション待機に入り、`REQ_HOLD` を送り始める
   (画面の MISSION が `ARMING` になる)
2. **SW_HOVER を GUIDED (上) にして、スロットルを 15% 以上へ**
   → 機体が GUIDED に入った瞬間に**競技時計が 0 から走り**、離陸が始まる
3. あとは全自動。画面の `MISSION` 行に `T+秒 / 残り秒` と今の段階が出る。

### 中断したいとき

| 操作 | 何が起きるか |
|---|---|
| PC の `[X]` | その場から自動着陸 (プログラムを打ち切って着陸段階へ) |
| **SW_HOVER を cen へ** | 即 POSHOLD (その場ホールド)。PC は指令を止めて手動へ譲る |
| **SW_HOVER を down へ** | 即 ANGLE (完全手動)。これが bail-out |
| **THR_CUT** | モーター停止。最終手段 |
| スロットルを 15% 未満へ | 高度もフローも手放して完全手動 |
| ロール/ピッチスティックを動かす | GUIDED から自動で抜けて POSHOLD へ |

### もう一度飛ばすとき

**THR_CUT でディスアーム → 再アーム。** アームの立ち上がりで待機に入り直す。
(アームしっぱなしのまま再開すると、着陸直後にまた離陸してしまうため、
エッジでしか始まらないようにしてある)

---

## 4. ミッションの中身と時間配分

### 4-1. なぜこの順番か

ルールブックは連続成功で **1.5 倍ずつ倍率が乗る**。だから順番は固定で、
最後の着陸が最も価値が高い。

| # | ミッション | 素点 | 倍率 | 実得点 |
|---|---|---|---|---|
| 1 | 滑走路内離陸 | 100 | 1.2 | 120 |
| 2 | 水平旋回 (連続2周) | 1000 | 1.2×1.5 | 1800 |
| 3 | 上昇旋回 | 1200 | 1.2×1.5² | 3240 |
| 4 | 8の字飛行 | 1400 | 1.2×1.5³ | 5670 |
| 5 | 着陸 (自動着陸滑走路) | 800 | 1.2×1.5⁴ | 4860 |

**着陸を落とすと 4860 点が丸ごと消える。** だから「途中の機動を 1 つ捨てても
時間内に降ろす」を最優先にしてある。

### 4-2. 時間の守り方 (3 段)

| # | 仕掛け | 何をするか |
|---|---|---|
| 1 | `Step.budget_s` | その段階を打ち切って**次へ進む**。粘ると後続と着陸を巻き添えにするため |
| 2 | `COMP_LAND_BY_S` (185s) | 何をしていても残りのミッションを捨てて**帰投・着陸**へ |
| 3 | `COMP_FORCE_LAND_BY_S` (205s) | 帰投を諦めて**その場で**降りる |

3 段目の理由: 離着陸エリア外の着陸は 800 → 300 点に落ちるが、飛んだまま
**3 分 55 秒 (235s) を迎えると着陸点は 0**。300 点を取りにいくほうが正しい。

### 4-3. 速度と半径の根拠

```
  旋回半径 R = v / ω        1周の時間 = 2πR / v
  v = 0.8 m/s, R = 1.5m  →  ω = 30.6 deg/s, 1周 = 11.8 秒
```

* ルールブックは「概ね 1.5m 以上」。実測の半径は指令より 2〜3 割大きく出る
  (2026-09-16: 指令 0.57m → 実測 0.7m) ので、1.5m 指令なら満たす側に外れる。
* 旋回に要るリーン角は `atan(v²/(R·g))` = **2.5°**。機体側の上限
  `FLOW_MAX_LEAN = 8°` に対して十分余裕がある。
* 2026-09-16 までの実機は v=0.4 m/s・1周 24 秒で、これだと機動だけで 216 秒。
  3 分に収まらないので倍速にした。機体側 `GUIDED_MAX_VEL` も 0.5 → 1.0 に
  上げてある (**ここでクランプされると半径が小さくなり、ルール違反側に外れる**)。

### 4-4. 触る場所

| 変えたいもの | どこ |
|---|---|
| 速度・半径・周回数・高度 | `utils/config.py` の `COMP_MANEUVER_SPEED` / `COMP_TURN_RADIUS_M` / `COMP_*_LAPS` / `COMP_CLIMB_ALT_M` |
| 各段階の制限時間 | `COMP_BUDGET_S` |
| 締切 | `COMP_LAND_BY_S` / `COMP_FORCE_LAND_BY_S` |
| 順番そのもの | `core/program.py` の `build_competition_program()` |
| 機体側の速度上限 | `flight_controller/include/quad/QuadConfig.h` の `GUIDED_MAX_VEL` (焼き直し要) |

変えたら **必ず** `cd position_estimator && python -m unittest tests.test_mission_sim` を
通すこと (順番・半径・想定合計・締切の整合・フェンス内かを機械的に検算する)。

---

## 5. 過去に踏んだ罠と、いま直っているか

| 症状 | 原因 | 現状 |
|---|---|---|
| 1 回目はラズパイの入力が来ない。main.py を再起動すると動く | 起動時に 3 秒のプローブを**1 回だけ**やり、外れると Camera2 が**ダミーに固定**されていた。2 人同時起動ではほぼ必ず外れる | `init/camera_setup.py` が最大 60 秒待つ。時間切れでも黙ってダミーにせず聞く |
| CALIB が 1 回失敗すると Camera2 が死ぬ | `_request_rpi_points()` にリトライが無かった | 5 回まで再試行 |
| 毎回 [M] を押さないと始まらない | 「GUIDED の立ち上がりで開始」にしていたが、機体は**新鮮な上りコマンドが無いと GUIDED に入れない**のに、PC は待機に入るまで何も送らない → 永久に発火しない | **アームの立ち上がり**で待機に入る。待機中は `REQ_HOLD` を送り続けるので、スイッチを上げた瞬間に GUIDED へ入れる |
| 機動が終わらないと後続が全部落ちる | 制限時間が無かった | 段階ごとの `budget_s` + 2 段の締切 |
| 着陸が「接地」で終わっていた | ルールは **5 秒以上の静止**が要る | `Phase.STILL` でカメラ位置が動かないことを 5 秒確認する |
| 8の字がフィールドに収まらない | 機首 +y だと 4R = 6.0m で幅ちょうど | 機首 +x (`YAW_INITIAL_ALIGN_DEG = 90`) + 起動時の幾何チェック |

---

## 6. まだ残っているリスク (実機で確認すること)

1. **v = 0.8 m/s は未飛行。** 実機の最速は 0.4 m/s までしか確認していない。
   初回は広い場所で `COMP_MANEUVER_SPEED` を 0.5 → 0.6 → 0.8 と上げて、
   円が崩れないこと・`fh_leanr/leanp` が飽和しないことを見ること。
   遅くする場合は 1 周が伸びるので、`COMP_BUDGET_S` と締切も見直す。
2. **上昇旋回の 2.2m で測距が届くか。** VL53L1X は明るい屋外だと遠距離が苦しい。
   失探しても機体は `RangeLost` で基準スロットルを保つので落ちはしないが、
   高度が流れる。`COMP_CLIMB_ALT_M` を下げるか、測距を SONAR に替える判断が要る。
   (`QuadConfig.h` の `RANGE_BACKEND`)
3. **離着陸エリアの位置。** 帰投先は「アーム中に地上でカメラが見ていた位置」。
   地上で機体が見えていないと、フィールド中心へ帰って降りる (着陸点が下がる)。
   準備時間の間に、機体が Camera1/2 の両方に映っていることを確認すること。
4. **想定 172 秒は理論値。** 移動の所要時間は過去ログ (WP 1m ≒ 6 秒) からの
   外挿で、4〜5m の移動は実測がない。初回は `T+` の進み方を見て
   `COMP_BUDGET_S` を詰め直すこと。
