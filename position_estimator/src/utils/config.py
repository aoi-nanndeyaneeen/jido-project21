# 定数、パス、フィールド設定などをまとめるモジュール

from pathlib import Path
import numpy as np

# ==========================================
# パス設定
# ==========================================
SRC_DIR  = Path(__file__).parent
ROOT_DIR = SRC_DIR.parent
LOG_DIR  = Path("C:/flight_logs")   # OneDrive外の安全なパス

# ==========================================
# カメラ・センサ設定
# ==========================================
CAMERA_ID   = 0
CAMERA_W    = 1920
CAMERA_H    = 1080
SERIAL_PORT = "COM7"
SERIAL_BAUD = 115200

# ==========================================
# フィールド設定
# ★ フィールド中心を原点(0,0)とした座標系
# ==========================================
FIELD_POINTS = np.array([
    [-8.0, -8.0, 0.0],
    [ 8.0, -8.0, 0.0],
    [ 8.0,  8.0, 0.0],
    [-8.0,  8.0, 0.0],
    [-8.0,  8.0, 2.0],
], dtype=np.float32)

# ==========================================
# X,Y推定の最低高度閾値
# この値未満では交点計算が発散するためスキップ
# ==========================================
MIN_TRACK_ALT = -0.5   # [m]

# ==========================================
# 外れ値フィルタ（フィールド±5m）
# ==========================================
VALID_X = (-13.0, 13.0)
VALID_Y = (-13.0, 13.0)
VALID_Z = ( -1.0, 20.0)

# ==========================================
# 3D表示の固定範囲
# ==========================================
VIEW_X = (-10.0, 10.0)
VIEW_Y = (-10.0, 10.0)
VIEW_Z = (  0.0, 15.0)