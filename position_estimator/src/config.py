# 定数、パス、フィールド設定などをまとめるモジュール

from pathlib import Path
import numpy as np

# ==========================================
# パス設定
# ==========================================
SRC_DIR  = Path(__file__).parent          # .../src/
ROOT_DIR = SRC_DIR.parent                 # .../position_estimator/
LOG_DIR  = ROOT_DIR / "logs"              # .../position_estimator/logs/

# ==========================================
# カメラ・センサ設定
# ==========================================
CAMERA_ID   = 0
CAMERA_W    = 1920
CAMERA_H    = 1080
SERIAL_PORT = "COM7"
SERIAL_BAUD = 115200

# ==========================================
# フィールド設定（4隅の3D座標）
# ==========================================
FIELD_POINTS = np.array([
    [ 0.0,  0.0, 0.0],
    [16.0,  0.0, 0.0],
    [16.0, 16.0, 0.0],
    [ 0.0, 16.0, 0.0],
], dtype=np.float32)

# ==========================================
# 外れ値フィルタ範囲（フィールドより少し広め）
# ==========================================
VALID_X = (-5.0, 21.0)
VALID_Y = (-5.0, 21.0)
VALID_Z = (-1.0, 20.0)

# ==========================================
# 3D表示の固定範囲
# ==========================================
VIEW_X = (-2.0, 18.0)
VIEW_Y = (-2.0, 18.0)
VIEW_Z = ( 0.0, 15.0)