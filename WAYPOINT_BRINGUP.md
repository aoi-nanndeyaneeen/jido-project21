# ウェイポイント飛行 立ち上げ手順

作成 2026-09-11。POSHOLD（フロー水平ホールド＋測距高度ホールド）が
飛べるようになった状態から、「ボタン一つで自動離陸 → 経路飛行 → 帰投 → 自動着陸」
までを段階的に通すための手順。

---

## 0. 設計の結論 — なぜ「PCからスティックを送る」を採らないか

最初に考えていた流れはこうだった：

> PCで自己位置推定 → 目標位置との差を出す → それを解消するスティック操作を送信

**この形は採らない。** 理由は3つあり、どれも実測値から出ている。

### (1) 姿勢ループにリンクの遅れを入れると発振する

IM920sL は半二重 19200bps、実効 15Hz、往復の遅れは 100〜200ms。
機体の姿勢ループは 1000Hz で回っている。ここへ 15Hz・遅れ 200ms の
角度指令を入れると、位相遅れがそのまま位相余裕を食う。
ゲインをどれだけ下げても、遅れ由来の発振は残る。

### (2) リンクが切れた瞬間に「最後の傾け指令」が残る

スティック指令は「傾け続けろ」という意味なので、電波が切れると
機体は最後の傾きのまま飛んでいく。切れたことを機体が気づくまでの
時間がそのまま暴走距離になる。

### (3) そもそも上りの経路が壊れていた

`flight_controller/include/Config.h` の `GroundData` は 42 + チェックサム4 =
**46 バイト**。IM920sL の実効ペイロードは 32 バイトで、超えた分は
モジュールが OK を返しながら黙って切り捨てる。
つまり `ap_roll` 以降（オフセット26バイト目〜）は**一度も機体に届いたことがない**。
`drone_s5.cpp` の `MODE_AUTO` は最初から動かないコードだった。

### 採った形

**PCは setpoint（目標値）しか送らない。**

```
  カメラ (tracker)   -> フィールド座標の位置 P [m]
  yaw_estimator      -> 機首方位 psi [rad]
        |
        v  position_estimator/src/core/mission.py   外側ループ 5Hz
   「機体座標の目標速度 [m/s]」 + 「目標対地高度 [m]」
        |
        v  IM920 上り 5Hz  (S5Cmd.h / 14+4=18 バイト)
        |
  機体 PosHold  : 目標速度 -> 速度PID -> 目標リーン角     内側 100Hz
  機体 AltHold  : 目標高度 -> 位置/速度PID -> スロットル   内側  50Hz
  機体 姿勢PID  : 目標リーン角 -> モーター                 最内 1000Hz
```

この形の良いところ：

- **GUIDED中に通る制御経路は POSHOLD と完全に同一。** 違うのは
  「目標速度と目標高度を、スティックが決めるか地上局が決めるか」だけ。
  つまり POSHOLD が飛べている限り、GUIDED も飛べる。
- **指令が途切れる = 速度指令 0 = その場ホールド。** 暴走する経路が無い。
- **自動離陸・自動着陸は「目標高度をスルーレートで動かす」だけ。**
  専用の制御モードを作らないので、「離陸のときだけ挙動が違う」という
  切り分け不能なバグが原理的に出ない。

---

## 1. 安全の骨組み（ここを崩さないこと）

GUIDED 中でも、以下はすべて生きている。上から順に強い。

| # | 操作 | 効果 |
|---|---|---|
| 1 | **THR_CUT** | 常にモーター停止。最終手段 |
| 2 | **SW_HOVER を下げる** | 即 ANGLE（完全手動）。bail-out |
| 3 | **SW_AUTO を下げる** | 即 POSHOLD（その場ホールド）。地上局だけ切れる |
| 4 | **ロール/ピッチスティックを動かす** | GUIDED から自動で抜けて POSHOLD へ |
| 5 | **スロットルスティックを 15% 未満に落とす** | 高度ホールドもフローも手放す（完全手動） |

### プロポの割り当て

`SW_AUTO` は **ch6**（`flight_controller/include/Config.h` の `enum Ch`）。
2ポジションスイッチを ch6 に割り当てる。`SW_HOVER` は ch8（3ポジション）、
`THR_CUT` は ch7 のまま。
（2026-09-12 追記: 当初 ch9 だったが、プロポ側で ch9 に割り当てられない
　ため ch6（未使用だった `SW_LEVEL` 枠）に変更した。）

> ⚠️ **スイッチの「上」がどちらかは、プロポの設定とリバース次第で逆になる。**
> コードの `up` は SBUS の生値が小さい側（`Ch_state()` は `des < -0.25` で `up`）。
> 2026-09-04 に、操縦者が ANGLE のつもりで 6 本すべて POSHOLD で飛ばしていた
> 事故がこれ。**必ず機体のシリアル画面で `MODE =` の表示を見て確認すること。**
> スイッチを動かして表示が期待どおりに変わってから飛ばす。

**スロットルスティックは GUIDED 中もパイロットが握り続ける。**
これは仕様であってバグではない。デッドマンスイッチとして機能する
（`FLOW_ENABLE_THR` / `ALT_ENABLE_THR` = 0.15）。
自動離陸させるときも、スロットルは 50% 付近に置いたままにする。
そこから先の実際の出力は機体の高度ループが決める。

リンク断のフェイルセーフ（`QuadConfig.h` § 9）：

- 1 秒 指令が来ない → 水平指令 0（その場ホールド）。高度目標は保持
- 4 秒 指令が来ない → 自動着陸を開始

一発で着陸させないのは、電波が瞬断するたびに降りてくる機体は
使いものにならないから。

---

## 2. 作業の流れ（この順で。飛ばさない）

### Phase 0 — 前提の確認（飛ばさない）

- [ ] POSHOLD が安定して飛べている（x, y, z すべて）
- [ ] `FLOW_BRINGUP_CHECKLIST.md` の Phase 1〜4 が済んでいる
- [ ] `position_estimator/src/utils/config.py` の `FIELD_PROFILE` が実際の
      フィールドと一致している。`small` は 1.4m × 1.4m
- [ ] ステレオキャリブレーションが済み、三角測量誤差が `TRIANG_WARN_M` 以下

### Phase 1 — カメラがドローンを検知できるか（機体は飛ばさない）

まずここ。位置が取れないと以降は全部意味が無い。

#### 検知方式: LED を「光点」として見る

機体に明るい点滅 LED を載せているなら、**動体検知ではなく輝度しきい値で拾う**。
人が暗がりで光点を見つけるのと同じ理屈で、これが一番確実。

従来の `motion`（背景差分）モードは、**静止ホバリング中に機体を見失う**。
合成画像での実測（60fps・60秒・`bg_learning_rate=0.0005`）:

| モード | 静止 0-10秒 | 静止 20-30秒 | 静止 50-60秒 | 点滅LEDの消灯フレーム |
|---|---|---|---|---|
| `motion` | 36.5% | **0.0%** | **0.0%** | 拾える |
| `bright` | 100% | 100% | 100% | 拾えない |
| `bright_or_motion` | 100% | 100% | 100% | 拾える |

学習率をほぼ 0 にしてあっても、MOG2 は数十秒で静止した物体を背景に吸収する。
ホバリングを追う用途に `motion` 単独は使えない。

`detection_params.json` の既定を **`"detect_mode": "bright_or_motion"`** にした。
点滅 LED なら、点灯フレームは輝度側が、消灯フレームは動体側（点滅そのものが変化）
が拾うので、穴が無い。

#### 露出を落とす

`bright` 系は「画面の中で LED だけが白飛びしている」状態で初めて効く。
明るいままだと白い壁・反射・照明も候補になる。

1. **キャリブレーション（5点クリック）は明るい状態で先に済ませる。**
   暗くすると隅や支柱が見えなくなってクリックできない
2. `config.py` の `TRACKING_EXPOSURE` に暗い値を入れる（`None` なら従来どおり自動）。
   Windows の UVC では概ね負値で、`-4` あたりから下げていく
3. 「LED 以外に白飛びしている物が無い」ところで止める。
   暗くしすぎると LED が小さく写って `bright_min_area_px`（既定 4）を割る

#### チェック

```
cd position_estimator/src
python main.py
```

- [ ] LED を点灯させたドローンを**手で持って**フィールド内を動かし、
      Camera 1 / Camera 2 の両方で検知枠が付くか
- [ ] **手を止めて 1 分放置しても検知が消えないか**（ここが本番。
      `motion` だと 20 秒で消える。`bright_or_motion` なら消えない）
- [ ] **プロペラが回っている状態**でも検知が続くか
- [ ] `in_dummy`（仮想円軌道へのフォールバック）に落ちないか
- [ ] `Velocity` ウィンドウのトップビューで、手の動きと同じ向きに点が動くか

#### 見失ったときに「最後の場所にいる」と推定すべきか

**してはいけない。** 最後の位置を持ち続けて位置ループを閉じると、
「推定器は大丈夫だと言っているのに機体は流れている」という、
一番たちの悪い壊れ方をする。壁に自信満々で突っ込む。

そして**その必要が無い。** 機体はオプティカルフローで自分の位置を保持できる。
カメラが見失ったときの正しい振る舞いは:

1. PC は速度指令 0（= `REQ_HOLD`）を送る
2. 機体は PosHold でその場に留まる（フローと測距だけで完結する）
3. 1.5 秒（`POS_LOST_LAND_S`）戻らなければ自動着陸

実装済みで、`mission.py` は `in_dummy` 中を `pos_valid=False` として扱う。
「見失った → その場で止まる」は**すでに人がやることと同じ**で、
「見失った → 見えていたつもりで飛び続ける」は人もやらない。

なお、1〜2 フレームの取りこぼしを埋める短時間の外挿は
`tracker.py` に `TRACK_COAST_SEC = 1.5` として既にある。これは
「時間制限つきで、かつ失探中だと分かる」形なので問題ない。
危ないのは無期限に外挿して、それを正常な観測として扱うこと。

### Phase 2 — 上りコマンドが機体に届くか（プロペラを外す）

ファームを両方焼く。

```
cd flight_controller  && pio run -e drone_s5   -t upload
cd ground_receiver    && pio run -e xiao_s5_log -t upload
```

- [ ] 機体を USB で PC に繋ぎ、`pio device monitor -e drone_s5` で画面を見る
- [ ] `s5_logger.py` は**起動しない**（USBポートが競合する）。
      代わりに `position_estimator/src/main.py` を起動する。
      起動ログに `[S5Link] 地上局に接続: COMxx` が出ること
- [ ] 機体側の画面に新設の行が出る：

```
CMD   rx: good=123 lost=0 badcs=0 badlen=0 badver=0  RSSI=..
         200 ms前  req=HOLD    vx=+0.000 vy=+0.000 alt=0.50 m flags=0x000B
GUIDED ----     phase=OFF     目標 vx=+0.000 vy=+0.000 alt=0.00 m (slew 0.00 m/s)
```

切り分け：

| 症状 | 原因 |
|---|---|
| `good` が 0 のまま、`badlen` も 0 | 無線が届いていない。GN/CH/ホップ（ENHP/DSHP）が両機で一致しているか |
| `badver` が増える | `S5Cmd.h` が機体側と地上側でずれている。両方コピーして焼き直す |
| `badcs` が増える | 電波が弱い。距離・アンテナ |
| `good` は増えるが `lost` が多い | 帯域。`TELEM_TX_HZ` をさらに下げる |

- [ ] `M` キーでミッションを開始してみる（アームしていないので何も動かない）。
      `phase=OFF` のまま、`req=HOLD` が届き続けることを確認

### Phase 3 — GUIDED に入れるか（プロペラを外す / ドライラン `'m'`）

- [ ] 機体側シリアルで `m` を押してドライラン ON（ESC へは 0 しか出ない）
- [ ] SW_HOVER=**down** でアーム（POSHOLD側ではアームできない仕様）
- [ ] スロットルを 30% 以上に上げる
- [ ] SW_HOVER を **up**、続けて SW_AUTO を **up**
- [ ] 機体画面が `MODE = GUIDED (地上局ガイド)` / `GUIDED ENGAGED` になるか
- [ ] PC 側に `[Mission] 機体が GUIDED に入りました -> 離陸` が出るか

入れないときは、機体画面の `GUIDED ---- 直前の解除理由:` を読む。
理由（ディスアーム / SW_AUTO が下 / フローが死んでいる / 測距が無い /
スティック操作を検出）がそのまま出る。

- [ ] ロールスティックを少し動かす → `GUIDED 解除: スティック操作を検出`
      が出て POSHOLD に戻るか（**この安全機能は必ず確認すること**）
- [ ] PC を Ctrl+C で落とす → 4 秒後に機体側が
      `!! 地上局リンク断 ... -> 自動着陸` を出すか

### Phase 4 — 自動離陸と自動着陸だけ（低空・広い床・指をスイッチに）

ウェイポイントは無効にして、離陸→ホバー→着陸だけを見る。
`position_estimator/src/utils/config.py` で：

```python
MISSION_WAYPOINTS = []          # WP なし。離陸 -> 帰投 -> 着陸だけになる
MISSION_TAKEOFF_ALT_M = 0.50
```

- [ ] ANGLE で普通に離陸し、安定ホバー
- [ ] SW_HOVER を up（POSHOLD で安定を確認）
- [ ] SW_AUTO を up → GUIDED
- [ ] `M` キー。目標高度へ 0.3 m/s で上がるか（`GUIDED_TAKEOFF_SLEW_MPS`）
- [ ] 2秒保持したあと自動着陸に入り、0.2 m/s で降りるか
- [ ] 対地 12cm を 400ms 下回ったところで出力が切れるか（`landed=1`）
- [ ] **指は常に SW_HOVER に置く。** 何かおかしければ下げる

### Phase 5 — 1点だけウェイポイント

```python
MISSION_WAYPOINTS = [(0.3, 0.0, 0.50)]     # 右へ 0.3m だけ
```

- [ ] 機首方位をどこから取るか。`config.py` の `MISSION_YAW_MODE`:
      - `"fixed"`（**既定**）… カメラのヨー推定が収束するまでは
        `YAW_INITIAL_ALIGN_DEG`（既定 0 = フィールド奥 +y）を機首方位として使う。
        収束したらカメラ側を優先する。
        **運用: 機首をフィールド奥へ向けて置いてアームし、ミッション中は機首を回さない。**
      - `"camera"` … 収束するまで水平移動しない（最も安全だが、下記の理由で
        最初の飛行では動き出さない）

> ⚠️ ヨー推定は「機体が 1 m/s 以上で動いている窓」が無いと収束しない
> （`YAW_MIN_DV_CAMERA`）。つまり **動かない → ヨーが決まらない → 動かせない**
> のデッドロックになる。だから最初の正方形飛行は `"fixed"` で通す。
> ヨーの自動補正を効かせるのは、正方形が安定して飛べるようになってからでよい
> （そのころには移動中の窓が取れるので、自然に収束するようになる）。
- [ ] 指令した向きへ動くか。**逆に動いたらその場で SW_HOVER を下げる**。
      逆になる原因は `body_frame_errors` の定義かヨーの符号。
      `mission.py` と `autopilot.py` の両方に同じ関数があるので、
      直すときは必ず両方
- [ ] 到達判定（0.20m）で止まり、2秒保持してから帰投・着陸するか

### Phase 6 — 正方形を1周

`MISSION_SQUARE_M = 0.80`（既定）に戻して `M`。
うまく行ったら `KP_POS` と `MAX_VEL` を少しずつ上げる。

---

## 3. 追加・変更したもの

| ファイル | 内容 |
|---|---|
| `flight_controller/include/S5Cmd.h` | **新規**。上りコマンドのパケット定義・送信・受信。32バイト制限内（14+4=18B） |
| `ground_receiver/include/S5Cmd.h` | 上と同一内容のコピー。**片方だけ直さないこと** |
| `flight_controller/include/S5Telem.h` | VERSION 4→5。`F_GUIDED` / `F_CMD_FRESH` / `F_LANDED` を追加 |
| `ground_receiver/include/S5Telem.h` | 同上 |
| `flight_controller/include/quad/QuadConfig.h` | § 9 を新設（GUIDED の全定数） |
| `flight_controller/include/quad/AltHold.h` | `commandTarget(高度, スルーレート)` を追加。自動離陸・着陸の実体 |
| `flight_controller/include/quad/PosHold.h` | `setVelCommand(vx, vy)` を追加。地上局の速度指令をスティック経路を通さず入れる |
| `flight_controller/src/drone_s5.cpp` | § 6-4 に GUIDED 状態機械。旧 `GroundData` 経路を撤去。`MODE_AUTO` を GUIDED として復活 |
| `ground_receiver/src/tools/s5_log.cpp` | PC からの `CMD,...` 行を 5Hz に間引いて無線へ中継。CSV に `guided/cmd_fresh/landed` 列を追加 |
| `position_estimator/src/core/s5_link.py` | **新規**。地上局 XIAO との双方向リンク（テレメトリ受信＋CSV保存＋コマンド送信） |
| `position_estimator/src/core/mission.py` | **新規**。ミッション状態機械と外側位置ループ |
| `position_estimator/src/app/main_loop.py` | `[M]` 開始 / `[X]` 中断 を配線 |
| `position_estimator/src/utils/config.py` | `GROUND_LINK_*` / `MISSION_*` / `TRACKING_EXPOSURE` / LED検知パラメータを追加 |
| `position_estimator/detection_params.json` | `detect_mode` と `bright_*` を追加。既定を `bright_or_motion` に |
| `position_estimator/src/core/camera.py` | 輝度しきい値検知（`_bright_mask`）を追加。`TRACKING_EXPOSURE` に対応 |
| `position_estimator/camera_server.py` | RPi 側（Camera2）にも同じ輝度しきい値検知を追加 |

ビルドは両方成功を確認済み（`drone_s5` / `xiao_s5_log`）。
ミッション状態機械は模擬機体で ARMING→TAKEOFF→WP×4→帰投→着陸まで
完走することを確認済み（実機での検証はこれから）。

---

## 4. 既知の注意点・未検証の部分

1. **`s5_logger.py` と `main.py` は同時に起動できない。**
   XIAO の USB シリアルは1プロセスしか開けない。テレメトリの CSV 保存は
   `S5Link` 側がやる（`position_estimator/src/logs/s5_link_*.csv`）。

2. **LED 検知は合成画像でしか検証していない。**
   静止ホバリングで `motion` が 20 秒で 0%、`bright_or_motion` が 100% という
   結果は合成画像（暗背景＋明るい光点）での実測。実機の会場の明るさ・
   LED の輝度・距離では、しきい値 `bright_threshold`（既定 230）と
   `TRACKING_EXPOSURE` の調整が必ず要る。Phase 1 でその場で詰めること。

   - LED を**常時点灯**にできるなら、そのほうが素直（消灯フレームが無い）。
     点滅のままなら `bright_or_motion` で穴は埋まる
   - 点滅周期がカメラのフレームレートに近いとエイリアシングで
     「ずっと消えている」状態が起きうる。速い点滅（30Hz以上）か常時点灯が安全

3. **高度の基準が2つある。** カメラの `z`（床を 0 とするフィールド座標）と、
   機体の測距による対地高度。床が平らならほぼ一致するが、
   ミッションの `z` は**対地高度として機体へ送っている**。
   床に傾斜や段差があるならここがずれる。

4. **機首方位は当面「フィールド奥で固定」で運用する。**
   `MISSION_YAW_MODE = "fixed"`（既定）。**機首をフィールド奥（+y）へ向けて
   置いてアームし、ミッション中は機首を回さない。**
   カメラのヨー推定が収束したら自動でそちらが優先される。

   ヨー推定が収束しないと動けない（= `"camera"` モード）とデッドロックになる
   のは、推定に「1 m/s 以上で動いている窓」が要るため。
   機体側の `g_yaw_est` はジャイロ積分なので時間とともにドリフトする。
   1〜2 分のミッションなら数度〜十数度で、1辺 0.8m の正方形には効かない量。
   **ヨーの自動補正を詰めるのは正方形飛行が安定してからでよい。**

5. **GUIDED から抜けると POSHOLD の保持高度 `ALT_TARGET_M`（0.50m）になる。**
   だから `MISSION_TAKEOFF_ALT_M` も 0.50 に合わせてある。
   離陸高度を変えるなら両方そろえること。片方だけ変えると、
   GUIDED を抜けた瞬間にその差だけ勝手に昇降する。

6. **フィールドが 1.4m × 1.4m だと、位置推定の誤差に対して経路が小さい。**
   既定の正方形は1辺 0.80m、到達判定は 0.20m。三角測量誤差が
   0.2m を超えるようなら、この経路は意味のある飛行にならない。
   広い場所（`FIELD_PROFILE = "large"`）に移すか、経路を拡大すること。
